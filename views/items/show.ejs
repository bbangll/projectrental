<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/form.css">
    <title>Document</title>
</head>
<body>
    <main>
        <div id="main">
            <div id="navbar">
                <h1 id="title">Buy Item.</h1>
                <a id="closeButton" class="squareButton" href="/items/new">
                    <img class="buttonIcons" src="/images/cancel.svg">
                </a>
            </div>
            <div id="line"></div>
            <h1> <%= item.itemName %> </h1>
            <h1> <%= item.itemPrice %> </h1>
            <h1> <%= item.itemLocation %> </h1>

            <form action="/items/<%=item._id%>/reviews" method="POST" id="newItem">
                <label>Quantity</label>
                <input type="number" name="quantity" class="input">

                <input type="submit" class="submitButton">
            </form>

            <% if (item.reviews.length) { %>
                <% item.reviews.forEach(r => { %>
                 <h1><%= r.quantity %> </h1>
                <% }) %>
            <% } %>
        </div>
    </main>
</body>
</html>